.private-messages-container
  %h3 Send Private Message

  - if flash[:alert]
    .alert.error
      = flash[:alert]

  = simple_format @pm.message

  = render partial: 'form', locals: { message: @reply }
